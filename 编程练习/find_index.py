a=[1,8,77,96,57]
b=77
c=88
def find(x,n):
    for i in range(len(x)):
        if x[i]==n:
            print(i)
            return i
    return -1
find(a,c)

'''
写一个函数，接收一个列表和一个目标值，返回目标值在列表中第一次出现的索引。
如果目标值不存在于列表中，返回 -1。
不能使用 list.index() 方法，必须自己写循环实现。

示例：
输入：([3, 8, 2, 10, 4, 7], 10) → 输出：3
输入：([3, 8, 2, 10, 4, 7], 5) → 输出：-1
'''
