等待机制选择：为什么最终统一用 presence_of_element_located 而不是 visibility_of_element_located
//////////////////////////////////////////////////////////////////////////////////
百度搜索框不可交互：为什么用 execute_script 而不是 send_keys 和 click
/////////////////////////////////////////////////////////////////////////////////
终端运行 WebDriverWait 与 allure 插件兼容性问题：如何用 PyCharm 配置解决
///////////////////////////////////////////////////////////////////////////////
POM 重构时模块导入报错：ModuleNotFoundError: No module named 'utils' 的排查过程
/////////////////////////////////////////////////////////////////////////
Allure 报告空白：如何定位是 allure-results 目录为空或工作目录错误



///////////////////////////////////
等待机制选择

现象：使用 visibility_of_element_located 等待百度搜索框时报 TimeoutException。

原因：百度搜索框在 DOM 中存在但不可见，visibility 要求元素可见，而 execute_script 不依赖可见性。

解决：改用 presence_of_element_located，只等待元素存在于 DOM。


///////////////////////////////////
百度搜索框不可交互

现象：send_keys 和 click 操作百度搜索框报 ElementNotInteractableException。

原因：百度对搜索框做了特殊限制，标准交互方法失效。

解决：统一使用 execute_script 设置 value 和触发 click。


/////////////////////////////////
终端运行 WebDriverWait 与 allure 插件兼容性问题

现象：终端运行 pytest 带 --alluredir 时，WebDriverWait 超时或报错。

原因：终端环境与 allure-pytest 插件存在兼容性冲突。

解决：改用 PyCharm 运行配置，添加 --alluredir 参数；命令行仅用于生成报告。


/////////////////////////////////
POM 重构时模块导入报错

现象：运行测试报 ModuleNotFoundError: No module named 'utils'。

原因：新建的 utils 包缺少 __init__.py，或包缓存未刷新。

解决：确保每个包目录都有空的 __init__.py；若刚创建报错，重新运行测试即可。


///////////////////////////////
Allure 报告空白

现象：生成 Allure 报告后页面空白。

原因：allure-results 目录为空（pytest 未正确输出结果）或工作目录错误。

解决：检查 pytest 运行配置的工作目录为项目根目录；确认 allure-results 中有 json 文件；使用 allure open 而非直接双击 html。